    cargo  new learn_module --lib
----------------

    cargo install cargo-modules

A cargo plugin for showing an overview of a crate's modules.

    cargo modules generate tree

     cargo modules generate tree --types --fns
